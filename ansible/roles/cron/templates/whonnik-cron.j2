###########################
### {{ ansible_facts['hostname'] }} ###
### {{ ansible_managed }} ###
###########################

# Automatically compress images inside samba shared
*/2 * * * * root /etc/mars-monitoring/scripts/compress-image.sh

# Automatically compress pdf to save space
*/5 * * * * root /etc/mars-monitoring/scripts/compress-pdf.sh

# [SAMBA SHARE] Backup
0 4 * * THU root /etc/mars-monitoring/scripts/samba-backup.sh
0 4 * * SUN root /etc/mars-monitoring/scripts/samba-backup.sh

# [Docker] Automatically update samba
30 4 * * * root cd /opt/stacks/samba && docker compose pull && docker compose up -d
